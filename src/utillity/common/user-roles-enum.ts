// import { RolesBuilder } from "nest-access-control";

export enum Roles {
  Admin = "admin",
  User = "user",
}
// export const roles: RolesBuilder = new RolesBuilder();
// roles.grant(Role.Admin).createOwn("User");
